[package]
name = "lubot-tasiyici"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true  # yayinlanmaz: kagit uzerinde de boyle
license-file.workspace = true
description = "The mapped weight container: a directory, a payload read in place, a depth ladder, and the ceiling an operator may declare."

[lints]
workspace = true

[dependencies]
lubot-nicem = { path = "../nicem", version = "0.1.0" }
sha2 = "0.10"
