[package]
name = "lubot-kanit"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true

# Self-contained: no dependencies, no filesystem access, no clock. A pure
# contract that can be checked by a test and nothing else.

[lints]
workspace = true
