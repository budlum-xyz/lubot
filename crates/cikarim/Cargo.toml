[package]
name = "lubot-cikarim"
version = "0.1.0"
edition = "2021"
description = "Lubot inference surface: score and rank with a trained checkpoint, never generate."
license = "PolyForm-Shield-1.0.0"
repository = "https://github.com/budlum-xyz/lubot"
readme = "README.md"

[dependencies]
lubot-egitim = { path = "../egitim", version = "0.1.0" }

[lints]
workspace = true
